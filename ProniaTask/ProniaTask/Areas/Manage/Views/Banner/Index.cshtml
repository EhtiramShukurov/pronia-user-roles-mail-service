
@{
    ViewData["Title"] = "Index";
}
@model ICollection<Banner>
<div class="container">
	<div class="card mt-5">
		<div class="table-responsive">
			<table class="table table-striped table-bordered">
				<thead>
					<tr>
						<th>Id</th>
						<th>Img</th>
						<th>Title</th>
						<th>SecondaryTitle</th>
						<th colspan="2">Operation</th>
					</tr>
				</thead>
				<tbody>
					@foreach (Banner s in Model)
					{
					<tr>
						<td>@s.Id</td>
						<td><img width="40" src="~/assets/images/banner/@s.ImageUrl"></td>
						<td>@s.Title</td>
						<td>@s.SecondaryTitle</td>
						<td><a class="btn btn-danger" asp-area="Manage" asp-controller="Banner" asp-action="Delete" asp-route-id="@s.Id">Delete</a></td>
						<td><a class="btn btn-info" asp-area="Manage" asp-controller="Banner" asp-action="Update" asp-route-id="@s.Id">Update</a></td>
					</tr>
					}
				</tbody>
			</table>
		</div>
	</div>
</div>